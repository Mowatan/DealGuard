[build]
builder = "DOCKERFILE"
dockerfilePath = "fouad-ai/backend/Dockerfile"
dockerContext = "fouad-ai/backend"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
